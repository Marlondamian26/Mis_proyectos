# =======================================================================
# NOTAS FINALES
# ========================================================================
# - Los servicios gratuitos de Render se suspenden tras 15 min de inactividad
# - Para despertarlos, acceder a la URL o usar "Resume Service"
# - PostgreSQL es OBLIGATORIO para evitar pérdida de datos
# - SQLite no se guarda entre despliegues/actualizaciones
# - Para configurar PostgreSQL:
#   1. Crear base de datos en Render (sección PostgreSQL)
#   2. Copiar la "Internal Database URL"
#   3. Añadir como DATABASE_URL en el entorno del backend
#   4. La primera vez que se despliegue con PostgreSQL, estará vacía
# - IMPORTANTE: Usar Web Service para el frontend (no Static Site) para que
#   funcione el refresh en todas las rutas de la SPA